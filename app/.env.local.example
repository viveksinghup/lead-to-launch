# Optional — Phase 1 scraping.
# Without this, Phase 1 serves the seeded demo data (12 Bandra dentists).
# Phases 2-5 run on your local Claude Code (no key here).
#
# Get a free token at https://apify.com (Settings → API tokens)
APIFY_TOKEN=
APIFY_ACTOR=compass~crawler-google-places

# Advanced (optional): override which Claude model the phases use.
# CLAUDE_CLI_PATH=/custom/path/to/claude
